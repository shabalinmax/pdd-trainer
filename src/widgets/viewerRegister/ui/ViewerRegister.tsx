import React from 'react';
import cls from './viewerRegister.module.scss';
const ViewerRegisterWidget = () => {
    return (
        <div>

        </div>
    );
};

export const ViewerRegister = React.memo(ViewerRegisterWidget);